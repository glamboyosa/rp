import { Mouse_Memoirs, Sofia } from "next/font/google";
const MM = Mouse_Memoirs({ subsets: ["latin"], weight: "400" });
const sofia = Sofia({ subsets: ["latin"], weight: "400" });

export { MM, sofia };
